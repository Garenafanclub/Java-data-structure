package Solid;

public class Main {
    public static void main(String[] args) {
        System.out.println();
        // map.getOrDefault(nums[i], 0):
        // This gets the current count of occurrences of the element nums[i] from the HashMap.
        // If the element is not present in the HashMap, it returns 0.

    }
}
